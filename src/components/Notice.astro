---
import { GithubSVG } from './Github';

type Props = { totalPosts: number }

const { totalPosts } = Astro.props;
---

<section class="flex flex-col gap-11 p-5 rounded-lg bg-white shadow h-[308px]">
    <div>
        <span class="text-gray-400">정리</span>
        <div class="flex flex-row mt-7">
            <div class="flex flex-col justify-end gap-2">
                <span class="text-sm">지금까지 기록한 날.</span>
                <span class="text-sm">기존 기록 공간.</span>
            </div>
            <div class="flex flex-col items-center gap-2">
                <span class="ml-8 underline underline-offset-4 decoration-2 decoration-sky-200">{totalPosts} 일</span>
                <a href="https://github.com/sjuhan123/habit-morning" class="ml-8 hover:text-sky-400">
                    <GithubSVG width={20} height={20} />
                </a>
            </div>
        </div>
    </div>
    <div>
        <span class="text-gray-400">목표</span>
        <div class="flex flex-row mt-7">
            <div class="flex flex-col justify-end gap-2">
                <span class="text-sm">기본 다지기와 Next.js 프로젝트</span>
            </div>
        </div>  
    </div>
</section>